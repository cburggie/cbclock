cbclock -- uses ncurses to display the date and time centered in your terminal.

Usage:
 $ cbclock
 exit with F2

Requirements:
 * gcc
 * libncurses

Build:
    $ make

 * if you want to add cbclock to your PATH:
    $ [sudo] make install
 * defaults to installing to $HOME/bin

 * if you don't have make:
    $ gcc -o cbc_main.o -c cbc_main.c -lncurses
    $ gcc -o cbc_util.o -c cbc_util.c -lncurses
    $ gcc -o cbc_data.o -c cbc_data.c
    $ gcc -o cbclock cbc_main.o cbc_util.o cbc_data.o





